{
  "id": "aify-service",
  "name": "Aify Service",
  "description": "Integration with the aify container service",
  "version": "0.1.0",
  "config": {
    "apiUrl": {
      "type": "string",
      "default": "http://service:8800",
      "description": "Service API URL (use Docker service name when in same network)"
    },
    "userId": {
      "type": "string",
      "default": "default",
      "description": "User identity for service requests"
    },
    "autoQuery": {
      "type": "boolean",
      "default": false,
      "description": "Automatically query the service for context before each agent turn"
    },
    "autoProcess": {
      "type": "boolean",
      "default": false,
      "description": "Automatically send conversation turns to the service for processing"
    },
    "debug": {
      "type": "boolean",
      "default": false,
      "description": "Enable debug logging"
    }
  }
}
